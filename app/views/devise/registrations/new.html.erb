<h4 class="page-header">Sign up</h4>
<div class="col-xs-12 col-md-6 col-lg-3">
<p>If you use indecent text or something that cause someone annoyed as username, motto.</p>
<p>We have the right to remove it.</p>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-group">
    <%= f.label :email %>
    <%= f.text_field :email, :autofocus => true, :class => 'form-control flat' %>
  </div>

  <div class="form-group">
    <%= f.label :username %>
    <%= f.text_field :username, :class => 'form-control flat', pattern: "[0-9a-zA-Z_]*", placeholder: "letters, digits and underscore only", onchange: "if ($('#user_username').val() === 'admin') { alert('Your login cannot be admin!'); }"%>
  </div>

  <div class="form-group">
    <%= f.label :nickname %>
    <%= f.text_field :nickname, placeholder: "DO NOT use Arabic", :class => "form-control flat" %>
  </div>

  <div class="form-group">
    <%= f.label :password %>
    <%= f.password_field :password, :autocomplete => "off", :class => 'form-control flat' %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation %>
    <%= f.password_field :password_confirmation, :autocomplete => "off", :class => 'form-control flat' %>
  </div>

  <div class="form-group">
    <%= f.label :name, "Name (This will be hidden to others.)" %>
    <%= f.text_field :name, :class => "form-control flat" %>
  </div>

  <div class="form-group">
    <%= f.label :school, "School and Department (This will be hidden to others.)" %>
    <%= f.text_field :school, :class => "form-control flat" %>
  </div>

  <div class="form-group">
    <%= f.label :gradyear, "Graduation Year of University (e.g. 103)" %>
    <%= f.number_field :gradyear, :class => "form-control flat" %>
  </div>

  <div>
    <%= f.submit "Sign up", :class => 'btn btn-info btn-lg' %>
  </div>
<% end %>


<%= render "devise/shared/links" %>
</div>
